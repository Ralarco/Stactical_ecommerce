/**
 * BetterAuth configuration.
 * This file configures BetterAuth with Prisma adapter.
 *
 * TODO: Configure when BetterAuth is installed and database is ready.
 */
export const authConfig = {
  // betterAuth({
  //   database: prismaAdapter(prisma),
  //   session: { strategy: 'jwt' },
  //   ...
  // })
};
